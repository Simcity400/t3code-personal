/**
 * Web's import path for the context-window derivations, which are shared with
 * mobile in `@t3tools/client-runtime/state/contextWindow`.
 */
export {
  deriveContextWindowSnapshotsByAgent,
  deriveLatestContextWindowSnapshot,
  formatContextWindowTokens,
  type ContextWindowSnapshot,
} from "@t3tools/client-runtime/state/contextWindow";
